# Jeton d'authentification clasp : NE JAMAIS committer
.clasprc.json
.clasp-*.json

# Autorisations de commandes Claude Code propres à CE poste : ne jamais les partager
# (un repo repris hériterait sinon d'autorisations comme "gh repo *" sans confirmation).
.claude/settings.local.json

# Dépendances Node
node_modules/

# Sauvegardes locales / fichiers système
*.bak
*.bak.html
.DS_Store

# Tout matériel sensible : les secrets vivent dans les Propriétés du script
# et dans 1Password, JAMAIS dans ce repo.
.env
.env.*
secrets*.json
*credentials*.json
client_secret*.json
*.key
*.pem
